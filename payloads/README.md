# Payloads

Permanent home for lesson payload JSONs (payload-mode builds).
Rule: payload JSONs are kept forever - they are the re-assembly
source whenever lesson_frame.html changes.
Tree mirrors the lesson folders:
  payloads/year-N/<strand>/<lesson_id>_payload.json
Committed via the normal close-out (lesson-gen -> main).
